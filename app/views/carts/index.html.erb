<h1>Cart Contents</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Price</th>
    <th>Count</th>
    <th></th>
  </tr>

  <% @carts.each do |article| %>
    <tr>
      <td><%= cart.name %></td>
      <td><%= cart.price %></td>
      <td><%= cart.count %></td>
      <td><%= link_to 'Show', cart_path(cart) %></td>
    </tr>
  <% end %>

  <%= link_to 'Back', articles_path %>
  
</table>
